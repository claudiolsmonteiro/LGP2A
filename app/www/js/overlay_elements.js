/**
 * Created by João on 09/03/2016.
 */

function overlay_elements_ready(){
    /*var dialog = document.querySelector('#test-dialog');
    if (! dialog.showModal) {
        dialogPolyfill.registerDialog(dialog);
    }

    $('#more-details-button').click( function(e) {
        $('#dialog-content').html('<a href="#/tabs">' + $(this).attr('data-obj') + '</a>');
        dialog.showModal();

    });

    $('#dialog-close-btn').click(function() {
        //console.log('click no close');
        dialog.close();

    });*/

};
